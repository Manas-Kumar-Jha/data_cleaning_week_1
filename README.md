# Week 1 – Data Cleaning and Exploration Using Pandas

# Objective: 
Learn Python basics and perform basic data exploration and cleaning using Pandas. Steps: 1.Load a CSV dataset into a Pandas DataFrame. 2.Explore data (head/tail, shape, columns, data types). 3.Handle missing values (identify, fill/drop). 4.Perform basic operations (filter rows, select columns). 5.Remove duplicates. 6.Create a derived column (total_amount = price * quantity). 7.Save the cleaned dataset as a new CSV file. Output: Jupyter Notebook (.ipynb) + cleaned CSV + brief summary."

# Activities Completed
1. Read the shopping dataset into Python using Pandas.
2. Explored the dataset to understand its structure and contents.
3. Located missing values and applied suitable methods to handle them.
4. Used Pandas functions to retrieve specific columns and filter data records.
5. Identified duplicate entries and removed them from the dataset.
6. Generated a new feature called total_amount based on existing data.
7. Stored the processed dataset in a new CSV file for future use.

# Software and Libraries Used
* Python
* Pandas
* NumPy
* Jupyter Notebook